<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.name %> Enrollment Details</title>
    <!-- Include Tailwind CSS styles -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Custom Fonts */
        body {
            font-family: 'Roboto', sans-serif;
        }

        h1, h2, h3, p {
            font-family: 'Poppins', sans-serif;
        }

        /* Custom Font Colors */
        body {
            color: #333333; /* Dark gray text color */
        }

        h1 {
            color: #1a202c; /* Dark gray heading color */
        }

        h2 {
            color: #2d3748; /* Gray heading color */
        }

        h3 {
            color: #3182ce; /* Blue heading color */
        }

        p {
            color: #4a5568; /* Blue-gray text color */
        }

        /* Custom Containers */
        .main-container {
            background-color: #ffffff; /* White background color */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        .enrollment-container {
            background-color: #ebf4ff; /* Light blue background color */
            margin-left: 10px; /* Adjust margin as needed */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        /* Custom Buttons */
        .custom-button {
            background-color: #4299e1; /* Blue button color */
            color: #ffffff; /* White text color */
        }

        .custom-button:hover {
            background-color: #3182ce; /* Darker blue on hover */
        }

    </style>
</head>

<body class="bg-gradient-to-r from-blue-400 via-indigo-500 to-purple-600 text-gray-800">
    <a href="/educator">
        <div class="flex items-center justify-right">
            <img src="https://cdn-icons-png.flaticon.com/128/3176/3176194.png"
                class="w-35 h-12 ml-6 my-3 flex items-center justify-middle" alt="view course">

            <div class="block bg-blue-500 text-white rounded-md py-2 px-4 hover:bg-blue-600 transition duration-300">
                Go to Dashboard
            </div>

        </div>
    </a>
    <div class="container mx-auto my-8 p-8 max-w-1/2 main-container">
        <div>
            <h1 class="text-5xl text-blue-600 font-semibold mb-4">
                <%= course.name %>: Enrollment Reports.
            </h1>
            <p class="mb-4 text-xl"><strong>Description:</strong>
                <%= course.description %>
            </p>
            <p class="mb-4 text-xl"><strong>Created By:</strong>
                <%= createdBy %>
            </p>
            <p class="mb-4 text-xl"><strong>Number of Enrollments:</strong>
                <%= enrollmentCnt %>
            </p>
        </div>
        <div class="enrollment-container">
            <h2 class="text-2xl font-semibold mb-4">Enrollment Percentage of <%= course.name %></h2>
            <!-- Circle Container -->
            <div class="relative inline-block w-20 h-20">
                <div class="w-23 h-23 absolute inset-0 rounded-full bg-green-100 border-4 border-green-500">
                    <div class="flex items-center justify-center h-full ">
                        <span class="text-gray-400 font-bold text-xl">
                            <%= percentage %>%
                        </span>
                    </div>
                </div>
            </div>
        </div>
        <div class="flex items-center">
            <h2 class="text-xl font-semibold mb-2">Enrollments:</h2>
            <table class="w-fit ml-10 border-collapse border border-gray-300">
                <thead>
                    <tr class="bg-gray-200 p-10 ">
                        <th class="p-5">S.no</th>
                        <th class="p-5">Name of student</th>
                        <th class="p-5">Email</th>
                        <th class="p-5">Date of Enrollment</th>
                    </tr>
                </thead>
                <tbody>
                    <% for( var i=0; i < enrolledstudents.length; i++) { %>
                        <tr class="<%= i % 2 === 0 ? 'bg-gray-100' : 'bg-white' %>">
                            <td class="p-5">
                                <%= i + 1 %>
                            </td>
                            <td class="p-5">
                                <%= enrolledstudents[i].name %>
                            </td>
                            <td class="p-5">
                                <%= enrolledstudents[i].email %>
                            </td>
                            <td class="p-5">
                                <%= enrollments[i].createdAt.toDateString() %>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>
