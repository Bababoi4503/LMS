<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Homepage</title>
    <!-- Include Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-r from-purple-100 to-purple-200">

    <%- include('header.ejs') %>

    <nav class="flex items-center justify-between px-6 py-4 bg-white shadow-md">
        <h1 class="text-2xl font-semibold">Welcome, <%= studentName %>!</h1>
        <div class="flex items-center space-x-4">
            <img src="https://cdn-icons-png.flaticon.com/256/535/535572.png" alt="Student Icon"
                class="w-8 h-8 rounded-full">
            <div>
                <p class="font-semibold text-gray-800"><%= studentName %></p>
                <p class="text-gray-600">Student ID: STU<%= studentId %></p>
            </div>
        </div>
    </nav>

    <!-- Student Homepage Container -->
    <div class="container mx-auto p-8">

        <!-- Enrolled Courses Container -->
        <div class="mb-8">
            <h2 class="text-2xl font-semibold mb-4">Enrolled Courses</h2>

            <% for (var i = 0; i < enrolledCourses.length; i++) { %>
            <!-- List of Enrolled Courses -->
            <div class="bg-white p-4 mb-4 border-purple-400 rounded-md w-full">
                <h3 class="text-xl font-semibold mb-2"><%= i + 1 %> : <%= enrolledCourses[i].name %></h3>
                <div class="flex items-center justify-end">
                    <a href="/scourse/<%= enrolledCourses[i].id %>"
                        class="w-36 h-12 bg-purple-500 text-white rounded-full  m-3 cursor-pointer hover:bg-purple-600 transition duration-300 flex items-center justify-center">
                        <img src="https://cdn-icons-png.flaticon.com/256/9930/9930152.png"
                            class="w-6 h-6 mr-2" alt="Start course">
                        Start Course
                    </a>
                </div>
            </div>
            <% } %>
        </div>

        <!-- Available Courses Container -->
        <div>
            <h2 class="text-2xl font-semibold mb-4">Available Courses</h2>
            <!-- List of Available Courses -->
            <% for (var i = 0; i < availcourses.length; i++) { %>
            <div class="bg-white p-4 mb-8 border-purple-400 rounded-md w-full">
                <h3 class="text-xl font-semibold mb-2"><%= availcourses[i].name %></h3>
                <p class="text-gray-600"><%= availcourses[i].description %></p>
                <div class="flex items-center justify-between mx-4 mt-4">
                    <!-- Enroll Box -->
                    <a href="/enrollCourse/<%= availcourses[i].id %>"
                        class="w-1/2 h-16 bg-purple-500 text-white rounded-full px-3 py-3 m-3 cursor-pointer hover:bg-purple-600 transition duration-300 flex items-center justify-center">
                        <img src="https://cdn-icons-png.flaticon.com/256/3422/3422947.png"
                            class="w-6 h-6 mr-2" alt="enroll course">
                        Enroll Course
                    </a>
                    <!-- View Box -->
                    <a href="/viewCourse/<%= availcourses[i].id %>"
                        class="w-1/2 h-16 bg-purple-500 text-white rounded-full px-3 py-3 m-3 cursor-pointer hover:bg-purple-600 transition duration-300 flex items-center justify-center">
                        <img src="https://cdn-icons-png.flaticon.com/256/4711/4711920.png"
                            class="w-6 h-6 mr-2" alt="view course">
                        View Course
                    </a>
                </div>
            </div>
            <% } %>
        </div>

        <%- include('footer.ejs') %>

    </div>

</body>

</html>
