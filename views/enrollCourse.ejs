<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= course.name %> - Enroll
    </title>
    <!-- Include Tailwind CSS styles -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-r from-blue-400 via-indigo-500 to-purple-600 text-gray-800">
    <div class="flex  items-center justify-center h-screen">
        <div class="bg-white w-1/2 p-8 rounded-md shadow-md text-center">
            <!-- Course Name -->
            <h1 class="text-5xl font-bold text-green-500 mb-4">
                <%= course.name %>
            </h1>

            <!-- Course Description -->
            <h3 class="text-lg mb-4">
                <%= course.description %>
            </h3>

            <!-- Confirm Enrollment Checkbox -->
            <label class="flex items-center space-x-5 mb-4">
                <input type="checkbox" class="form-checkbox h-4 w-4" required>
                <span class="text-xl"> Enroll</span>
            </label>

            <!-- Enroll Course Button -->
            <form action="/enrollCourse/<%= course.id %>" method="post">
                <button type="submit"
                    class="w-full bg-purple-500 text-white rounded-md py-2 hover:bg-blue-600 transition duration-300">
                    Enroll
                </button>
            </form>
        </div>
    </div>
</body>

</html>