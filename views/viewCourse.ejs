<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.name %> - View Course</title>
    <!-- Include Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-r from-gray-100 to-gray-300 flex items-center justify-center h-screen">

    <div class="container w-2/3 mx-auto p-8 bg-white rounded-lg shadow-lg flex flex-col items-center">

        <!-- Course Name -->
        <h2 class="text-4xl text-indigo-600 font-semibold mb-6">
            <%= course.name %>
        </h2>

        <!-- Course Description -->
        <p class="text-lg text-gray-700 mb-6">
            <%= course.description %>
        </p>

        <!-- List of Chapters -->
        <div class="p-6 w-full bg-gray-100 rounded-md shadow-md">
            <h2 class="text-2xl font-semibold mb-4">Chapters:</h2>
            <ul>
                <% for(var i=0; i < chapters.length; i++) { %>
                    <li class="mb-2 text-lg border-b border-gray-300 py-2">
                        Chapter <%= i + 1 %>: <%= chapters[i].name %>
                    </li>
                <% } %>
            </ul>
        </div>

        <!-- Enroll Course Button -->
        <div class="mt-6">
            <a href="/enrollCourse/<%= course.id %>"
                class="inline-block w-48 h-12 bg-indigo-500 text-white rounded-md px-3 py-3 text-center hover:bg-indigo-700 transition duration-300">
                <h3 class="text-lg font-semibold">Enroll Course</h3>
            </a>
        </div>
    </div>

</body>

</html>
